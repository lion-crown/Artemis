# AGENTS.md

本仓库中 AI 编码代理的工作指南。

## 协作原则

- 动笔前说明假设；存在影响范围或行为的歧义时先澄清。
- 仅修改解决当前任务所需的内容；不要顺手重构、清理无关文件或扩大范围。
- 多步骤工作应明确“步骤 → 验证”；完成时提供实际验证结果。
- 未经要求不提交、不推送、不删除用户数据。

## 项目概览

Artemis 是自托管、多用户、多代理的 AI 助手平台。项目由 Python 后端、React Dashboard、CLI 与桌面端组成；默认不依赖外部队列。

主要目录：

```text
src/          后端与已构建的 Dashboard 资源
dashboard/    Dashboard 前端源码
desktop/      桌面端与便携包脚本
docker/       容器部署文件
docs/         用户与开发文档
tests/        单元与集成测试
```

## 模块边界

依赖必须向内流动：Dashboard 通过 HTTP 调用 API；API 调用领域层；领域层调用工具与数据访问层。

- `infra/`：领域逻辑、数据库、Agent、网关和连接器。
- `api/`：路由、认证、请求校验、响应映射；不得承载领域规则。
- `cli/`：命令行体验；复用领域服务，不复制业务逻辑。
- `dashboard/`：前端源码；组件不得直接请求网络，统一经类型化 API 层调用。
- `launch.py`：启动组合根；不放路由、SQL 或命令参数解析。

硬性规则：领域层不得依赖 API、CLI 或启动层；API 不得依赖 CLI 或启动层；数据访问层只处理持久化。

## 开发与验证

```text
make install-hooks
make all
make lint
make typecheck
uv run pytest -m "not live"
uv run pytest tests/unit -x -q
uv run pytest tests/integration -x -q
cd dashboard && npx tsc --noEmit
make build-frontend
```

- 使用 `uv run pytest`，不要直接调用 `pytest`。
- 修改前端源码后，按需执行类型检查和前端构建；不要直接编辑已构建资源。
- 修改用户可见文本时同步维护中英文资源，并运行对应本地化测试。
- 修改数据库结构时同时提供两种数据库方言的迁移，并更新迁移版本测试。
- 默认交付门槛是 `make all`；若环境或外部依赖阻塞，报告具体命令、现象和已通过的专项验证。

## 实现约束

- Python 使用 asyncio；除明确的执行器边界外不得在 async 路径增加阻塞 I/O 或线程。
- 文件内容经工作区抽象层读写；不要绕过其路径与后端规则。
- 所有用户可见的服务端文本使用本地化资源；中英文键结构必须一致。
- 跨平台测试不得假定 POSIX 文件系统、根目录、命令路径或权限语义。
- 连接器、浏览器与外部服务调用必须保持认证和所有权检查。

## 工作流

1. 阅读相关实现、测试与文档，确认边界。
2. 用最小改动实现需求，并添加或更新直接覆盖该行为的测试。
3. 运行与改动风险相称的验证；前端、API、本地化和数据库分别执行对应检查。
4. 检查变更范围与格式；说明未运行或未通过的验证及原因。

## 沟通

- 默认使用中文，先给结论再给细节。
- 引用代码时给出准确路径和行号。
- 不把内部实现名、历史命名或兼容细节作为对外品牌描述。
